export type Product ={
        id: number;
        name: string;
        price: 38.0;
        description: string;
        imageUri: string;
}

export type OrderLocationData = {
        latitude:number;
        longitude:number;
        address: string;
}